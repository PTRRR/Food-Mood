import { load } from 'worker-timers-broker';
import { worker } from './worker/worker';
var blob = new Blob([worker], { type: 'application/javascript' });
var url = URL.createObjectURL(blob);
var workerTimers = load(url);
export var clearInterval = workerTimers.clearInterval;
export var clearTimeout = workerTimers.clearTimeout;
export var setInterval = workerTimers.setInterval;
export var setTimeout = workerTimers.setTimeout;
//# sourceMappingURL=/users/chrisguttandin/repositories/worker-timers/node_modules/tsconfig-holy-grail/src/module.js.map